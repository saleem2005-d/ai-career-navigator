spring:
datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/career_navigator}
    username: ${SPRING_DATASOURCE_USERNAME:engine_admin}
    password: ${SPRING_DATASOURCE_PASSWORD:super_secure_vault_password_2026}
    driver-class-name: org.postgresql.Driver
jpa:
    hibernate:
    ddl-auto: update
    show-sql: true
    properties:
hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

gemini:
api:
    key: ${GEMINI_API_KEY:your_fallback_api_key_here}

server:
port: 8080